# Coded by SpiderX360

# import numpy as np

arr = ['1', '2', '3', '1', '2', '3', '4']

# print(sorted(arr))
unique = np.unique(arr)
unique = ['1' '2' '3' '4']
# print(unique)

if len(arr) %2 != 0:
    for i in range(len(unique)):
        if (unique[i][0]) == arr[:]:
            print("Matched", unique[i])
            i += 1
